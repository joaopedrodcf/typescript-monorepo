{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        "outDir": "dist",
        "rootDir": "src",
        "types": ["node"],
        "skipLibCheck": true // prevent errors in node things
    },
    "exclude": ["node_modules", "dist", "**/tests/*"],
    "references": [{ "path": "../app" }]
}
