{
  "name": "dogbook",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^4.2.2"
  },
  "scripts": {
    "build": "lerna run build --stream",
    "clean": "lerna run clean --parallel",
    "watch": "tsc -b -w --preserveWatchOutput"
  }
}
